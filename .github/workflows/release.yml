name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: sxzz/workflows/.github/workflows/release.yml@dd67f194be6388e12ee61ad0cd3b81cf74bfd294 # main
    with:
      publish: true
    permissions:
      contents: write
      id-token: write
